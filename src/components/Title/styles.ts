import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  Title: {
    color: 'black',
    fontSize: 30,
    fontWeight: 'bold',
  },
  containerTitle: {
    // marginTop: 40,
    // marginBottom: 30,
  },
});
