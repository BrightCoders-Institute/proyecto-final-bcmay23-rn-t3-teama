import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginLeft: 15,
    marginTop: 10,
  },
  ingredientsText: {
    fontSize: 16,
    marginTop: -15,
    color: 'black',
  },
  });