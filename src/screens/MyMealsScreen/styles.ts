import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        // marginTop: 5,
    },
    calendarContainer: {
        backgroundColor: 'white',
        height: 180,
        elevation: 5,
        shadowColor: 'black',
        marginBottom: 10,
    },
    dayTitle: {
        fontWeight: 'bold',
        marginTop: 15,
        fontSize: 22,
        marginLeft: 25,
    },
});
